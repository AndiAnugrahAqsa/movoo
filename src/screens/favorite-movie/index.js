import React from "react";
import { View, Text } from "react-native";
import Layout from "../layout"

const Index = ({navigation}) => {
    return (
        <Layout navigation={navigation}>
            <Text>Ini Screen Favorite Movie</Text>
        </Layout>
    )
}

export default Index;